# Server — Gemini API key (kept secret, never sent to the browser)
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=
PORT=3001

# Optional: override Gemini model
# GEMINI_MODEL=gemini-2.5-flash

# Frontend — optional Uzbek text-to-speech (https://developer.kotib.ai)
VITE_KOTIBAI_API_KEY=
